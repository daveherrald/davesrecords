# TypeScript Check

Run TypeScript compiler to check for type errors.

## Instructions

Run `npx tsc --noEmit` and report results.

If errors are found, list them with file paths and line numbers.
If no errors, confirm the codebase is type-safe.
